# Адрес API. По умолчанию /api/v1 — в разработке проксируется на backend,
# и origin один, поэтому CORS не участвует вовсе.
VITE_API_URL=/api/v1

# Куда проксировать /api в режиме разработки.
VITE_BACKEND_ORIGIN=http://127.0.0.1:8000
