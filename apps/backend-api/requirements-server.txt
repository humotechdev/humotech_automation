# Зависимости, нужные только серверу приложений в контейнере.
#
# Отдельным файлом, а не в requirements.txt: gunicorn — процесс-менеджер
# на fork(), и на Windows он ставится, но не работает. Разработчик здесь
# запускает `manage.py runserver`, и класть в общий список пакет, который
# на его машине бесполезен, значило бы обещать то, чего нет.
#
# Устанавливается только в образе backend (см. Dockerfile).

-r requirements.txt

# WSGI-сервер. Django development server не рассчитан на нагрузку и сам
# об этом предупреждает: он однопоточный по замыслу и не переживает
# ни медленного клиента, ни отвалившегося соединения.
gunicorn~=23.0
