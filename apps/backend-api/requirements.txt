# Боевые зависимости backend-api.
#
# SQLAlchemy и Alembic здесь НЕТ намеренно: активный runtime работает
# на Django ORM и Django migrations. Прежний слой сохранён в `src/`
# и `migrations/` только как история и перекрёстная проверка поведения;
# зависимости для его запуска вынесены в requirements-legacy.txt.

Django~=5.2
djangorestframework~=3.16
django-filter~=25.1
psycopg[binary]~=3.2

# AI-модуль
pgvector~=0.4
openai~=1.60
tiktoken~=0.8
# Pydantic остаётся там, где он к ORM не привязан: структурированный ответ
# модели, проверка ответов провайдера и схемы конфигурации.
pydantic~=2.9
pydantic-settings~=2.6

# Пароли: argon2 — стойкий алгоритм по умолчанию, см. PASSWORD_HASHERS.
argon2-cffi~=25.1

# Выгрузки. CSV отдаётся потоком силами stdlib; XLSX без библиотеки
# не собрать, а кадровики работают в Excel и просят именно его.
openpyxl~=3.1

# Схема OpenAPI. Поведение API не меняет: строится по маршрутам и
# сериализаторам при обращении к /api/schema. По ней фронтенд CRM
# генерирует типы, поэтому схема доступна и в бою, а интерактивные
# читалки (Swagger, ReDoc) — только при DEBUG.
drf-spectacular~=0.30

# Печатное заявление. Больничный и отпуск оформляются бумагой с
# подписью, и человек должен получить готовый бланк, а не набирать
# его заново. Кириллица требует встроенного TTF — шрифт берётся из
# системы (`fonts-dejavu-core` в образе), в репозиторий не кладётся.
reportlab~=4.2
